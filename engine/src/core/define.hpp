#pragma once

#include <string>

enum KeyCode{
    KEY_A,
    KEY_D,
    KEY_S,
    KEY_W,
    KEY_SPACE
};

enum Button{
    BUTTON_L,
    BUTTON_R
};

#define WIDTH 1280
#define HEIGHT 768

const int LINE_SIZE = 256;
